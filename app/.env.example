# ── Production Backend (S10 T1) ─────────────────────────────────────────────
# Base URL of the production NestJS backend. Empty in dev (Vite proxies /api).
VITE_API_URL=https://medini-crm-production.up.railway.app